# Copy to scripts/local.env (git-ignored) and adjust: personal paths used by the test scripts.
# Folder holding cod_lnxded, main/ and the mod dir. WSL scripts need the /mnt/c form,
# docker/run.sh the Windows form - set the one you use.
export COD1_SERVER_DIR="/mnt/c/path/to/your/server"
