#ifndef COD1PLUS_HITBOX_DRAW_H
#define COD1PLUS_HITBOX_DRAW_H

/* Draw the SERVER's tested head box as debug lines. OFF unless
 * COD1RELOADED_HITBOX_DRAW=1. Dev use only - see hitbox_draw.c. */
void hitbox_draw_init(void);

#endif
